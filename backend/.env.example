# Server
PORT=5000
NODE_ENV=development

# PostgreSQL (default)
DB_USER=
DB_HOST=localhost
DB_NAME=collaborative_todo
DB_PASSWORD=
DB_PORT=5432

# Optional: use SQLite instead by setting DB_TYPE=sqlite
# DB_TYPE=sqlite

# JWT
JWT_SECRET=your_32+_char_secret_here
JWT_EXPIRES_IN=1h

# Frontend URL
CLIENT_URL=http://localhost:3000