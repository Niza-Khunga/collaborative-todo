# Server
PORT=5000
NODE_ENV=development

# Database (PostgreSQL)
DB_USER=your_db_user
DB_HOST=localhost
DB_NAME=collaborative_todo
DB_PASSWORD=your_db_password
DB_PORT=5432

# Optional: Uncomment for SQLite (ignore PostgreSQL vars above)
# DB_TYPE=sqlite

# JWT
JWT_SECRET=your_strong_jwt_secret_here_min_32_chars
JWT_EXPIRES_IN=1h

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:3000

node_modules/
.env
npm-debug.log
coverage/
.DS_Store

# backend data
data/